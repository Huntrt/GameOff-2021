(Improve to able to export keycode as JSON try to use)

This the hotkey package make by Huntrt:
	> Add "Hotkeys.cs" to an gameobject (recommend on gamemanager)

	> Add or modify as munch of keycode variable in "Hotkeys.cs" if needed (recommend line 20)

	> Add "KeyInfo.cs" to the button that assign key, it name need to match it keycode variable name
	Example: Button gameobject name "Run" need to be same name as "Run" variable in "Hotkeys";

	> "KeyInfo.cs" need to has an text display by using option in the script

	> Add BlockInput prefab at the last child of any canvas then assign it into "Hotkeys.cs" (optional)